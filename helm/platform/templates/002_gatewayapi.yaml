apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: {{ .Values.gateway.name }}
  namespace: {{ .Values.apps.namespace }}
spec:
  gatewayClassName: {{ .Values.gateway_class.name }}
  listeners:
    - name: http
      port: 80
      protocol: HTTP
    # - name: https
    #   port: 443
    #   protocol: HTTPS
    #   tls:
    #     mode: Terminate
    #     certificateRefs:
    #     - kind: Secret
    #       name: my-app-tls-secret
    #       namespace: {{ .Values.apps.namespace }}